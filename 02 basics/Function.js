function demo(name) {
    return name.toUpperCase();
}
demo("sakkmask");
var loginUser = function (uname, email, isPaid) {
    if (isPaid === void 0) { isPaid = false; }
    console.log(uname);
    return uname;
};
loginUser("dsmjlnkl", "sadnjdk2", true);
